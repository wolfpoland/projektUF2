@section scripts{
    <script type="text/javascript">
        $(function () {
            $.getJSON('/api/Kontakty', function (contactsJsonPayload) {
                console.log(contactsJsonPayload);
                $(contactsJsonPayload).each(function (i, item) {
                    console.log(item.imie);
                    $('#produkty').append('<li>' + item.Imie + ' ' + item.Nazwisko + '</li> ');
                });
            });
  

        });
   
        $('#saveContact').click(function () {
            console.log("Poszlo");
            $.post("/api/Kontakty",
                  $("#saveContactForm").serialize(),
                  function (value) {
                      alert(value);
                      $('#produkty').append('<li>' + value.imie + ' ' + value.nazwisko + '</li>');
                  },
                  "json"
            );
        });

        function formatItem(item) {
            return item.Imie + ' ' + item.nazwisko;
        }
        function find() {
            var id = $('#szukanie').val();
            $.getJSON('http://localhost:2470/api/Kontakty' + '/' + id).done(function (data) {
                $('#kontakt').text(formatItem(data));
            });
            $('#klik').click(function () {
                console.log('Poszlo klik szukania');
                find();
            });

        }

        /* 
            
        
        
        
            $('#saveContact').click(function () {
            $.post("api/contact",
                  $("#saveContactForm").serialize(),
                  function (value) {
                      $('#contacts').append('<li>' + value.Name + ' ' + value.Surname + '</li>');
                  },
                  "json"
            );
        });


          $('#save').click(function () {
            console.log('Klik');
            $.post("/api/Kontakty/Post", $('#formularz').serialize(),
                function (value) {
                    alert(value);
                    $('#produkty').append('<li>' + value.imie + '</li>');
                },
                "json"
                );
        
        
        $(document).ready(function(){
            $.getJSON('http://localhost:2470/api/Kontakty').done(function (data) {
                console.log(data);
                $.each(data, function (key, item) {
                    var n = 0;
                    for (var no in item) {
                        console.log("w petli petli: " + item[n]);
                        n++;
                    }
                    console.log('czytam:' + item[key]);
                    $('<li>', { text: formatItem(item) }).appendTo($('#produkty'));
                });
            });
            function formatItem(item) {
                console.log('Imie: ' + item.id);
                return item.id + " " + item.imie + " " + item.nazwisko;
            }
            <form id="saveContactForm" method="post">
    <h3>Create a new Contact</h3>
    <p>
        <label for="contactId">Contact Id:</label>
        <input type="text" name="Id" />
    </p>
    <p>
        <label for="contactName">Contact Name:</label>
        <input type="text" name="Name" />
    </p>
    <input type="button" id="saveContact" value="Save" />
</form>
        });*/
    </script>


}


<div class="jumbotron">
    <ul id="produkty"></ul></br>

    <input type="text" id="szukanie"/>
    <input type="button" value="Szukaj" id="klik" />
    <p id="kontakt"/>



</div>

<form id="saveContactForm" method="post">
  
    <p>
        <label for="contactId">Id:</label>
        <input type="text" name="Id" />
    </p>
    <p>
        <label for="contactId">Imie:</label>
        <input type="text" name="Imie" />
    </p>
    <p>
        <label for="contactName">Nazwisko:</label>
        <input type="text" name="Nazwisko" />
    </p>
    <input type="button" id="saveContact" value="Save" />
</form>
